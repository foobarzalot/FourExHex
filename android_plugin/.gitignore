# Gradle build artifacts for the RotationFix plugin AAR. The Kotlin source and
# build scripts are tracked; the compiled output (and the AAR staged into
# addons/rotationfix/bin/, ignored via the root bin/ rule) is regenerated by
# tools/build_android_plugin.sh.
.gradle/
build/
